let restaurantNames = ["KFC", "Momo Paradise", "Nabezo"];
let promotedNames = [];

for (let i = 0; i < restaurantNames.length; i++) {
    promotedNames.push("Restaurant Name: " + restaurantNames[i]);
}

console.log(promotedNames);
